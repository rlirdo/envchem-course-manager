# SELFTEST — 環境化學課程管理系統 驗收自測報告

- 受測檔案：`index.html`（單一檔、無外部相依）　大小 **463,608 bytes（約 453 KB）**
- 測試日期：2026/08/22
- 測試方式：
  1. **語法檢查**：以 Python 抽出 `<script>` 內容 → `node --check` → **rc = 0，無語法錯誤**（Node v24.19.0）。
  2. **邏輯自動測試**：以 Node + 極簡 DOM／localStorage stub 載入同一份 JS，直接呼叫 `ROSTER` / `STORE` / `ACT` / `VIEWS` / `ROUTER`，共 **46 項檢查全數通過（46 passed, 0 failed）**。
  3. **真實瀏覽器實測**：本機起 `python -m http.server` 後於瀏覽器載入，實際操作路由、貼上匯入、點名、簡報播放器、練習交卷與 RWD（375 px）。

> 測試腳本位於暫存區（`harness.js` / `test.js` / `build.py` / `check.py`），非交付物，未寫入本資料夾。

---

## 總結

| 項目 | 結果 |
|---|---|
| SPEC §10 驗收 A–J | **全部通過** |
| 額外檢查（備份／範本／清除／邊界值／學習單連結） | **全部通過** |
| `node --check` | **通過（rc=0）** |
| 真實瀏覽器渲染與互動 | **通過**（console 無錯誤） |
| 待瀏覽器人工確認項目 | 3 項（列印排版、剪貼簿、檔案下載，見文末） |

---

## A. 名單：貼 15 位學生＋標題列「座號 班級 姓名」

**做法**：貼上第 1 列為 `座號 班級 姓名`，其後 15 列 `01 環科三甲班 王小明` 形式。

| 檢查 | 結果 | 實測 |
|---|---|---|
| A | ✅ PASS | 學生數 = 15；訊息 = 「第 1 列看起來像標題列，已略過」＋「匯入 15 位」 |
| A-2 | ✅ PASS | 第一位解析 = `{seat:"01", cls:"環科三甲班", name:"王小明"}` |

> 訊息兩句都有顯示，且座號保留字串 `"01"`（未被轉成數字 1）。

## B. 貼「01 Yosi Takun」

| 檢查 | 結果 | 實測 |
|---|---|---|
| B | ✅ PASS | `{seat:"01", cls:"", name:"Yosi Takun"}` — **班級為空、姓名完整併回**，未把 `Yosi` 誤判為班級 |
| B-2 | ✅ PASS | 「班級 姓名」→ `環科三甲班 / 陳小美`；「姓名」→ `單名同學`（班級空） |
| B-3 | ✅ PASS | `05,環科三甲班,趙小天,轉學生`（半形逗號）→ 座號/班級/姓名/備註 皆正確 |

**規則實作位置**：`ROSTER.parseLine()` 先以 `/[\t,，、]/` 切；`parts.length <= 1` 時才改以 `/[\s　]+/` 切；`ROSTER.assign(parts, bySpace)` 中 `bySpace === true` 走「剩下全部併回姓名」路徑（`rest.join(' ')`）。

## C. 同批再貼一次

| 檢查 | 結果 | 實測 |
|---|---|---|
| C | ✅ PASS | 訊息 =「匯入 0 位」＋「**略過 15 位重複**」；名單仍為 15 位，未產生重複列 |

> 重複判定鍵 = `班級 + 姓名`（SPEC §4「同班級同姓名」）。

## D. 匯出 CSV 以 BOM 開頭

| 檢查 | 結果 | 實測 |
|---|---|---|
| D | ✅ PASS | 名單 CSV 首字元 = **U+FEFF**；檔名 `學生名單_115-1.csv` |
| D-2 | ✅ PASS | 出席 CSV、課程規劃表 CSV、練習成績 CSV **首字元皆為 U+FEFF** |

**實作**：所有匯出統一走 `U.downloadCSV()`，內含 `'﻿' + U.csv(rows)`。

## E. 日誌：進第 1 週全部預設出席；改 2 人請假

| 檢查 | 結果 | 實測 |
|---|---|---|
| E-1 | ✅ PASS | 進入第 1 週 → 出席 15/15、未點名 0（`STORE.ensureWeekDefault` 於首次進入時建立） |
| E-2 | ✅ PASS | 兩人各按兩下（出席→缺席→請假）後：出席 13、請假 2；T=1 → 材料費由 **16 份 → 14 份**（同步變動） |
| E-3 | ✅ PASS | 出席率 = **86.7%**，分母 = 15 —— **請假與缺席都留在分母內** |
| E-4 | ✅ PASS | 指導老師 1／2 兩欄皆清空時：T 暫以 **2** 計，畫面出現紅字「請先到總覽基本資料填指導老師姓名」 |

**摘要列實際輸出**（瀏覽器實測，3 人班級）：
```
本週出席 3 人／共 3 人（缺席 0、請假 0）　→　加上授課教師 1 位＝材料費 4 份
點一下第一位後 →
本週出席 2 人／共 3 人（缺席 1、請假 0）　→　加上授課教師 1 位＝材料費 3 份
```
格式與 SPEC §6(3) 完全一致。

## F. 15 人只點 10 人

**做法**：按「清除本週」→ 逐一點 10 人（未點名 → 出席）。

| 檢查 | 結果 | 實測 |
|---|---|---|
| F | ✅ PASS | 清除後未點名 = 15；點 10 人後 → 出席 10、未點名 5；畫面顯示紅字「**還有 5 位未點名，這週的數字先不算數**」 |
| F-2 | ✅ PASS | 該週不計入全班累計出席率（已完成點名週數 = 0） |

**「預設全出席」與「未點名」如何並存**：`log[w].init` 旗標。首次進入該週 → 全員設為出席並把 `init = true`；「清除本週」把 `att` 清空但**保留 `init = true`**，因此重新進入不會再自動全到；新增的學生在既有（已 `init`）週次不會被補預設，狀態為「未點名」。

## G. 總週數改 28

| 檢查 | 結果 | 實測 |
|---|---|---|
| G-1 | ✅ PASS | 課程規劃表列數 = **28**，`course.totalWeeks = 28` |
| G-2 | ✅ PASS | 教室日誌週次下拉含 `<option value="28">第 28 週` |
| G-3 | ✅ PASS | 列印課程規劃表頁尾含「**共 28 週**」 |
| G-4 | ✅ PASS | 列印授課時數表頁尾含「**共 28 週**」 |
| H-3 | ✅ PASS | 輸入 61（超出 1–60）被 alert 擋下並還原 |

## H. 授課時數表合計＝實際加總

| 檢查 | 結果 | 實測 |
|---|---|---|
| H | ✅ PASS | W1 時數 3 → **2.5**：課程規劃表合計 68 → **67.5**；授課時數表列印之合計欄 = **47.5**，與「有填日期或有點名紀錄」週次的實際加總完全相符（非寫死） |
| H-2 | ✅ PASS | 輸入 2.3（非 0.5 倍數）→ alert 擋下並還原為 3 |

> 授課時數表只列出「有填日期或有點名紀錄」的週次（本例 28 週中列出 18 週有日期者，合計 47.5），合計以 `rows.reduce(...)` 即時計算。

## I. 學生視角與成績代碼

| 檢查 | 結果 | 實測 |
|---|---|---|
| I-1 | ✅ PASS | `#/week/13` 畫面**不含** `nav class="tabs"`（管理分頁），且有「回學生入口」 |
| I-1b | ✅ PASS | W13 連結 `worksheet-ai-module1a.html` |
| I-1c | ✅ PASS | W14 連結 `worksheet-ai-module1b.html` |
| I-2 | ✅ PASS | `#/practice/13` 亦不含管理分頁 |
| I-3 | ✅ PASS | 座號／姓名未填 → 顯示「請先填寫座號與姓名，才能交卷」，無法交卷 |
| I-4 | ✅ PASS | 全對交卷 → 得分 **100**，產生成績代碼（160 字元 base64） |
| I-5 | ✅ PASS | 代碼解碼 = `{v:1, week:13, seat:"03", name:"林大同", score:100, detail:[...], ts:"2026/08/22 13:53:05"}` |
| I-6 | ✅ PASS | 老師端貼入代碼匯入 → 總覽練習得分表出現「林大同 W13 = 100」 |
| I-7 | ✅ PASS | 再匯入同生同週 40 分 → 得分表**仍取最高分 100** |
| I-8 | ✅ PASS | 座號 99「不存在同學」→ 列入**待處理**清單並紅字提示 |
| I-9 | ✅ PASS | `#/student` 不含管理分頁 |
| I-10 | ✅ PASS | 無 hash → 進老師模式，六分頁導覽齊備 |

**瀏覽器端真實交卷驗證**（非 stub）：於 `#/practice/13` 填入座號 07／吳建宏、全選正解、按交卷 →
畫面顯示 100 分，`codeBox` 代碼解出 `{v:1,week:13,seat:"07",name:"吳建宏",score:100,detail:[1×10],ts:"2026/08/22 13:54:12"}`，
同時 `STORE.d.scores` 寫入 1 筆（同機自動進總覽）。

## J. 教材份量

| 檢查 | 結果 | 實測 |
|---|---|---|
| J-1 | ✅ PASS | 每教學週簡報 **≥12 張**（見下表） |
| J-2 | ✅ PASS | **200 張投影片全部**都有備忘稿逐字稿；字數 min=118／中位數=170／max=226 字，皆為一句一行、第一人稱口語 |
| J-3 | ✅ PASS | 每教學週練習 **≥8 題**（實際 9–10 題） |
| J-4 | ✅ PASS | 每教學週教材重點卡 **6–10 張**（實際 7–10 張） |
| J-5 | ✅ PASS | 考試週 W11／W17：模擬題各 10 題、考試範圍各 8 條、應考須知 7／8 條 |
| J-6 | ✅ PASS | 假日週 W4／W8 顯示放假卡 |

各教學週明細（重點卡 / 投影片 / 練習題）：

```
W1 (10/14/10)  W2 (8/12/9)   W3 (9/14/10)  W5 (8/14/10)  W6 (9/15/10)
W7 (9/15/10)   W9 (9/15/10)  W10(8/14/9)   W12(9/15/10)  W13(9/16/10)
W14(9/15/10)   W15(8/14/9)   W16(8/14/9)   W18(7/13/9)
```

- 投影片總計 **200 張**（含考試週與假日週以外的 14 個教學週）
- 題目總計 **155 題**（14 教學週 × 9–10 題 ＋ 2 考試週 × 10 題模擬題）

---

## 額外檢查（SPEC §9 備份與設定、邊界條件）

| 檢查 | 結果 | 實測 |
|---|---|---|
| X-1 | ✅ PASS | 完整備份 JSON 含 `schemaVersion: 1` 與全部學生／點名／成績 |
| X-2 | ✅ PASS | 課程範本 `_type:"ecm_template"` **只含 course + plan**，不含學生／點名／成績，且清空指導老師人名 |
| X-3 | ✅ PASS | 清除全部資料（雙重確認）後回復預設，學生 0 位 |
| X-4 | ✅ PASS | 匯入備份後完整還原 15 位學生 |
| X-5 | ✅ PASS | localStorage 單一物件 key = `ecm_data` |

## 真實瀏覽器實測補充

| 項目 | 結果 |
|---|---|
| 頁面載入、console 錯誤 | **無任何 console 錯誤** |
| 六分頁導覽渲染 | 總覽／學生名單／課程規劃表／課程內容／教室日誌／備份與設定 全部正常 |
| 簡報播放器 | `#/week/13` 播放器出現，頁碼「2 / 16」，下一頁正常，逐字稿區塊 149 字 |
| 教材卡 | W13 渲染 9 張 `.mcard` |
| RWD（375×812 手機） | 課程規劃表／週教材／總覽三個頁面 **body 水平溢出皆為 0 px**（寬表格於 `.tblwrap` 內自行捲動） |
| 貼上匯入（瀏覽器實際操作） | 「01 Yosi Takun」在真實 DOM 中同樣解析為 座號 01／班級空／姓名 Yosi Takun |
| 點名三態與摘要列 | 點擊 `.attbtn` 後摘要列與材料費份數即時更新 |

---

## 待瀏覽器（人工）確認的項目

以下三項在 Node stub 中已驗證「會被正確呼叫、傳入的 HTML 內容正確」，但**最終呈現需人工開啟確認**，原因是它們依賴瀏覽器的原生對話框或系統行為，無法自動化斷言：

1. **列印版面的實際 A4 分頁效果**（`列印學生名單／課程規劃表／授課時數表／課程紀錄／簡報逐字稿`）
   - 已驗證：`U.print()` 會開新視窗並寫入含 `@page{size:A4}`、黑白友善樣式、頁尾產出日期的完整 HTML；內容字串經正規表示式比對正確（含「共 28 週」與動態合計）。
   - 待人工確認：實際列印預覽的分頁位置、表格是否有跨頁斷列（已加 `break-inside:avoid`）、字級是否適讀。
   - **注意**：瀏覽器可能阻擋彈出視窗，第一次使用需允許本站彈出視窗（程式已有 alert 提示）。

2. **剪貼簿複製**（成績代碼一鍵複製、快速連結複製）
   - 已驗證：按鈕會呼叫 `U.copy()`，且有 `execCommand('copy')` 後備路徑。
   - 待人工確認：`navigator.clipboard` 在 `file://` 或非 HTTPS 環境可能不可用，需確認後備路徑在該環境下有效（GitHub Pages 為 HTTPS，預期正常）。

3. **檔案下載**（CSV／JSON 備份／範本）
   - 已驗證：`U.download()` 產生 `Blob` 與 `<a download>` 並觸發 click，內容（含 BOM）正確。
   - 待人工確認：瀏覽器實際落檔檔名與編碼，以及 Excel 開啟 CSV 時中文與座號 `01` 的顯示（系統內已於學生名單頁標註提醒，並建議用列印輸出附件一）。

此外，**匯入備份的檔案選擇（`<input type="file">`）** 在 Node 中以貼上 JSON 路徑替代驗證（`ACT.applyImport` 已 PASS）；選檔流程本身需人工點選確認。
